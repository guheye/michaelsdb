# Copy to .env.local and fill in values. Never commit .env.local.

# Required for AI headline rewriting (Anthropic API).
ANTHROPIC_API_KEY=

# Optional: Unsplash API key for backfilling article images when sources lack OG images.
UNSPLASH_ACCESS_KEY=
